=== .run/env.sh
# generated by <ROOT>/manager/tools/bash/runner - regenerated on every run
export PATH=<PATH>
export GM_OS=<OS>
export TOOLS_DIR=<ROOT>/manager/tools/bash/runner
export MANAGER_DIR=<ROOT>/manager
export GM_LIB_DIR=<LIB>
export RUN_DIR=<ROOT>/manager/tools/bash/runner/.run
export GM_WAIT=<LIB>/waitport.sh
export GM_TAILER=<LIB>/tailer.sh
export GM_WAIT_TIMEOUT=180
export PROJECT=fixture
export INFRA_DIR=<ROOT>/infra
export INFRA_ENV_FILE=.env.test
export INFRA_LABEL=shared\ mysql
export GM_APP_ENV=.env.local
=== .run/docker-1-infra.sh
#!/usr/bin/env bash
# generated by <ROOT>/manager/tools/bash/runner - regenerated on every run
. <ROOT>/manager/tools/bash/runner/.run/env.sh
trap 'trap - HUP TERM; kill 0' HUP TERM
trap ':' INT
printf "\033]0;%s\007" 'infra · mysql 20010'
echo "───── infra · mysql 20010 ─────"
cd '<ROOT>/infra'
docker compose --env-file "$INFRA_ENV_FILE" up
__st=$?
echo
echo "[infra] stopped (status $__st) · ↑ re-runs it · exit closes the pane"
trap - INT HUP TERM
exec bash --rcfile <ROOT>/manager/tools/bash/runner/.run/rc/docker-1-infra/bashrc -i
=== .run/docker-2-api.sh
#!/usr/bin/env bash
# generated by <ROOT>/manager/tools/bash/runner - regenerated on every run
. <ROOT>/manager/tools/bash/runner/.run/env.sh
trap 'trap - HUP TERM; kill 0' HUP TERM
trap ':' INT
printf "\033]0;%s\007" 'api · :20200 (docker)'
echo "───── api · :20200 (docker) ─────"
"$GM_WAIT" mysql 127.0.0.1 20010 || true
cd '<ROOT>/api/Docker/Main'
docker compose --env-file '.env.local' up --build
__st=$?
echo
echo "[api] stopped (status $__st) · ↑ re-runs it · exit closes the pane"
trap - INT HUP TERM
exec bash --rcfile <ROOT>/manager/tools/bash/runner/.run/rc/docker-2-api/bashrc -i
=== .run/docker-3-client.sh
#!/usr/bin/env bash
# generated by <ROOT>/manager/tools/bash/runner - regenerated on every run
. <ROOT>/manager/tools/bash/runner/.run/env.sh
trap 'trap - HUP TERM; kill 0' HUP TERM
trap ':' INT
printf "\033]0;%s\007" 'client · :20221 (docker)'
echo "───── client · :20221 (docker) ─────"
"$GM_WAIT" api 127.0.0.1 20200 || true
cd '<ROOT>/client/Docker'
docker compose --env-file '.env.local' up --build
__st=$?
echo
echo "[client] stopped (status $__st) · ↑ re-runs it · exit closes the pane"
trap - INT HUP TERM
exec bash --rcfile <ROOT>/manager/tools/bash/runner/.run/rc/docker-3-client/bashrc -i
